car<-lm(Price~Age+KM+HP+cc+Doors+Gears+Quarterly_Tax+Weight,data=Toyota)
summary(car)
pred<-predict(car,data.frame(Age=22,KM=40000,HP=100,cc=1500,Doors=1,Gears=3,Quarterly_Tax=150,Weight=1000))
summary(pred)
pairs(Toyota)
cor(Toyota)
plot(car)
residualPlots(car,tests=F)
avPlots(car)
qqPlot(car)
influenceIndexPlot(car)
dataframe<-data.frame(Toyota,"predict-values"=predict(car),"Errors"=car$residuals)
pred1<-predict(car)